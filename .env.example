POSTGRES_PASSWORD=parcourse
DATABASE_URL=postgresql+psycopg://parcourse:parcourse@db:5432/parcourse
VITE_API_BASE_URL=http://localhost:8000
JWT_SECRET=
# Fernet key encrypting stored API keys at rest. Generate your own for production:
# python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=
