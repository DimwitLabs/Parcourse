POSTGRES_PASSWORD=parcourse
DB_SCHEMA=public
LOG_LEVEL=info
BACKEND_PORT=8000
FRONTEND_PORT=5173
VITE_API_BASE_URL=http://localhost:8000
CORS_ORIGINS=["http://localhost:5173"]
JWT_SECRET=
ENCRYPTION_KEY=

# Only for an external database. Leave unset and the app builds this from
# POSTGRES_PASSWORD; setting it here means the two can disagree.

# DATABASE_URL=postgresql://user:password@host:5432/postgres
