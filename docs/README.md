# Parcourse Docs

The documentation site, built with Docusaurus and styled to match the app.

```bash
npm install
npm start
```

`npm run build` writes a static site to `build/`, and `npm run serve` serves it.

The version pill in the navbar reads `../frontend/package.json`, so the number
follows the app without being written down twice.
